# ASR 处理流实现记录

用户已要求实现处理流并引入 ASR，基于此前认可的 OBS-ASR 复用方案。本轮边界为完成片段导入到持久化转写查询，保留现有 UI，不部署未经真实联调的服务。

- [x] 核对参考分支公司 submit/query/result 契约与测试。
- [x] 先添加导入幂等、秒级对齐、区间、续跑和失败测试。
- [x] 实现 FFmpeg 提取与 45 秒音频块；SQLite 持久化任务、有限重试及租约。
- [x] 按参考实现公司 ASR 适配，允许 result 缺少顶层 code。
- [x] 实现 Bearer 查询接口、限时媒体签名、Range 回放读取。
- [x] 用 FFmpeg 与本机 HTTP 模拟服务验证，无外部 ASR 调用。
- [x] 补齐启动、配置、时间单位、精度与未完成范围说明。

后续真实联调需要公司 ASR 可访问的音频服务 HTTPS 地址、服务地址及必要凭据。OBS 片段完成 hook 和部署 UI 的业务 API 连接尚未安装；不将这一版本描述为全链路生产完成。
